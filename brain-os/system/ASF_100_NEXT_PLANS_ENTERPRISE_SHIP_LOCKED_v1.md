# ASF — 100 next plans · enterprise ship (LOCKED v1)

**Saved:** 2026-06-16T05:49:57Z · **Retrofit:** doc-datetime-law batch retrofit
**Version:** 1.0 · **Locked:** 2026-06-13 · **Promoted to root:** 2026-06-15 (was wrongly filed under `archive/attachments/` — **active queue, not archive**)  
**Authority:** ASF order  
**Router:** `SINA_GOVERNANCE_ENTRY_LOCKED_v1.md` · **Index:** `SINA_AUTHORITY_INDEX_MAP_LOCKED_v1.md` row `ASF_100_NEXT_PLANS`  
**Rule:** Green UI without disk receipt = **NOT DONE**. Form closed without `canvas_form_apply_picks_v1.py` SHIP = **NOT LAW**.

**Conduct law:** `SOURCEA_NO_FAKE_PROGRESS_ENTERPRISE_SHIP_LOCKED_v1.md` — no fake progress; proven evidence · meaningful effect · result-driven · enterprise-grade ship only.

**North star:** Controlled receipt layer · W1 filmed · W2 PASS · W3 first invoice · **zero** enterprise risk from fake completion.

---

## A. Kill fake green (1–15) — do first or everything else lies

1. Run `canvas_form_apply_picks_v1.py --dry-run` on M1 Canvas — list every applied ID with **no** LOCKED file change.
2. Run `canvas_form_apply_picks_v1.py --apply` — only rows with `confirmed=true` and still in open set.
3. Re-open M1 rows where you picked RECONCILE/DEFER but need plain-English rewrite (`Q-WIRE-G3`, `ENF-20`, `Q-FR-1013f0`, `7.05`).
4. Remove from `canvas-form-picks-applied-v1.json` any ID that never shipped — **or** ship it same day.
5. Hub → Safety check — record PASS/FAIL receipt; ignore green form wire alone.
6. Ban “form 0 open” as success metric in any agent reply — metric = **SHIP receipt path**.
7. Hub edit workspace posts one table: Applied ID | Pick | Disk file changed | Y/N.
8. Worker one sa: register orphan `SINA_WORKER_INCIDENT_028_REPEAT_REPORT` in authority index — AS bundle green.
9. Fix S10 broker timeout — unblocks honest self-heal loop.
10. Validate `validate-run-inbox-disk-truth-v1.sh` assertion — factory truth wired.
11. Stop all chats from running `apply` without Canvas Submit proof in jsonl.
12. Judge Center re-run after apply — 0 ACTIVE_STALE required before W1 film claim.
13. Thread Room index any chat claiming “shipped” without path — T30 continuity.
14. Delete any remaining scratch `*founder*form*.canvas.tsx` if found — M1 only.
15. ASF rule locked: **PICK on Canvas → Apply script → Validator PASS → then green.**

---

## B. W1 — demo proof (16–30) — enterprise door opens here

16. Film 5-min demo: AI action → BLOCK → valid commit → receipt logged.
17. Film tamper beat: edit receipt → validator **FAIL**.
18. Save demo video to TrustField vault + RESEARCH trace.
19. Run `validate-demo-enforcement-v1.sh` on camera — show PASS in frame.
20. Wire Hub “Commit” button to `sourcea_execute_v1.py --demo-enforcement` if not live — SinaaiDataBase hub-edit workspace only.
21. One-page buyer PDF: **what BLOCK/ALLOW means** — no repo paths — Commercial vault.
22. Remove “full autopilot” from all external copy — semi-auto L1 only.
23. W1 script plain English — Gov review vault gate before send.
24. Design partner list: 5 names max — Commercial — no blast.
25. W1 falsifier written: if demo fails once → no outbound that week.
26. Receipt JSON sample packaged for NDAs — TrustField lane.
27. Map W1 to `SOURCEA_UNIFIED_PORTFOLIO_COMMERCIAL_SSOT_LOCKED_v3.1.md` § demo-for-sale — one sentence.
28. No new pre-LLM modules for W1 — use existing kernel only.
29. W1 done receipt: `~/.sina/enforcement/w1-film-receipt-v1.json`.
30. Hub hero may say W1 done **only** after receipt exists.

---

## C. W2 — honest factory proof (31–40)

31. Keep factory **FREEZE** until W1 receipt filed.
32. Honest counter must stay `unproven_done: 0` — daily check.
33. One Rail A worker run with broker boundary test — Claude Q1 proof.
34. Log broker reject of malformed prompt — jsonl receipt.
35. `execution.json` + `next-execution-pointer-v1.json` single pointer — Brain reconciles.
36. No AUTO-RUN until kill flag green + W1 receipt + ASF tap.
37. sa-0798 close or Brain re-picks — no zombie act.
38. REGISTRY row for W2 PASS in Master Operating Tracker.
39. W2 buyer message: “616 honest steps, not 1000 fiction.”
40. Archive any hero copy still saying “prove RT LIVE” when receipt PASS — cosmetic scrub on hub projection only.

---

## D. W3 — first money · non-risky (41–55)

41. Pick **one** lane only: TrustField MSB/consulting **or** Noetfield advisory — not both this month.
42. Commercial vault: one SOW template — Gov W3 contract gate YAML applied.
43. First outreach: **10** contacts max — agentic lane — founder never cold email.
44. OpenRouter/spend: Grade D until verified — no burn planning.
45. Partner track (Voyage/P05) before Inception if Commercial v3 order kept.
46. Invoice path: TrustField entity only — no Noetfield money ledger fantasy.
47. W3 falsifier: no deposit by date X → kill parallel outreach scripts.
48. CRM row: name · stage · receipt · next tap — hub or vault only.
49. No pricing on web until counsel checklist PASS — Gov vault.
50. First call script: governance wedge — not “AI runs your company.”
51. Deposit definition: signed SOW or Stripe link paid — binary.
52. W3 weekly: 3 receipts or stop — ENFORCEMENT-6MO law.
53. Research Brief refresh  matrix — R2 one session.
54. Kill Polsia-speed copy everywhere — category analog only.
55. W3 receipt file: `~/.sina/commercial/w3-first-deposit-receipt-v1.json`.

---

## E. Form & law SHIP — picks that must become disk (56–70)

56. `Q-JUDGE-STACK-v1` A → ship `SINA_JUDGE_STACK` wiring if not already — Worker.
57. `Q-THREAD-ROOM-v1` A → run `thread_room_run_v1.py` index — receipt.
58. `Q-AGENT-MEMORY-ENFORCE` YES → session gate on spine — Brain v2 P0.
59. `ENF-04` APPROVE → materialize judge scripts if missing validators.
60. Plain-English rewrite pass on **all** ENF rows you flagged “BAD WORDING” — Canvas regen + hub apply.
61. `7.07` NEVER → remove bad question from generator — never show again.
62. `2.02` C → document choice in LOCKED form law §ANSWERED.
63. `Q-FR-002-WTM` START → Brain assigns one WTM sa — not narrative.
64. `11.01–11.05` SHIP → each must map to one merged LOCKED clause or archive — no orphan SHIP.
65. Form generator sync: `generate_integrity_canvas_form_data_v1.py` after every law touch.
66. M1 Canvas Submit only — M3 generates data, does not own picks.
67. Hub shows **count + link** — never full question list as SSOT.
68. Intake queue: new conflicts → `form-open-intake-v1.json` → Canvas row before chat debate.
69. Dual-pick gate: hub hero must match form JSON or FAIL loud.
70. ASF: no new form rows until 56–69 green for 48 hours.

---

## F. Enterprise product · non-risky SKUs (71–85)

71. **TrustField — Controlled Receipt Audit** — fixed scope · fixed price · 2-week delivery.
72. **TrustField — RPAA readiness pack** — document-only · no payment rails in product.
73. **Noetfield — Authorization pack** — TLE-style evidence · advisory not SaaS claim.
74. **Noetfield — Board/compliance readout** — one PDF · one call.
75. Kill SKU: “Trust Ledger as money movement” — void forever.
76. Kill SKU: “SourceA as consumer app” — internal spine only.
77. Enterprise buyer: MSB/CISO/compliance officer — not indie hacker.
78. Copy test: would counsel say no? If yes → don’t ship copy.
79. SOC2 path: document gap list — defer certification sell until W3.
80. One landing page per SKU max — TrustField.ca — no ten sites.
81. Demo env: local only until W1 film — no public bypass URL.
82. Contract: liability cap + AI disclaimer — Gov template only.
83. Data: Canada residency statement — accurate or omit.
84. Reference: one design partner logo only with written OK.
85. VC pack vault index stays **vault-only** until W1+W3 receipts exist.

---

## G. Agents & machines — one owner (86–95)

86. Brain: pick · reconcile · handoff — **zero** implement files.
87. SinaaiDataBase hub-edit workspace: apply · FR-003 · hub/panel — **not** a scoreboard agent.
88. Canvas data · mx E2E — **no** picks · **no** scratch canvas.
89. Worker: one sa · validators · receipts — **no** P0 hero edits.
90. Commercial: vault · outreach copy — **no** sa assignment.
91. Gov: constraints YAML — **no** build orders.
92. R2: external facts — **no** roadmap reorder.
93. R1: briefs — **no** “fix everything” in chat.
94. M1: search only — never live ops.
95. Parallel “unify all docs” forbidden — GOV_UNIFY batch queue only.

---

## H. Proof you’re enterprise-ready (96–100)

96. Single page **Ship Scorecard**: W1 · W2 · W3 · Form SHIP · AS bundle · Demo PASS — all Y/N.
97. Weekly ASF tap: one PICK from scorecard only — not 100 chat threads.
98. If any scorecard N → no new SKUs · no new LOCKED law · no market reports.
99. First enterprise pilot: one customer · one receipt chain · one testimonial — then scale talk.
100. This file is the queue — **not** chat. Brain assigns sa from rows 1–15 until green, then 16–30, then 41–55.

---

## Execution order (mandatory)

```text
1–15  Kill fake green
16–30 W1 film + demo receipt
31–40 W2 honest factory (FREEZE until W1)
41–55 W3 first money (one lane)
56–70 Form picks → disk SHIP
71–85 Enterprise SKUs (sell only what counsel allows)
86–95 Agent boundaries
96–100 Scorecard gate
```

**Founder:** Canvas PICK · Hub Safety · no Terminal.  
**Everything else:** assigned sa with path in WORKER_ROUND_REPORT.

---

*End ASF_100_NEXT_PLANS_ENTERPRISE_SHIP_LOCKED_v1*
