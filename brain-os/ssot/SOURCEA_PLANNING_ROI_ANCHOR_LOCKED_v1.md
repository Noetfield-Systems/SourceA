# SourceA — Planning ROI Anchor (LOCKED v1)

**Saved:** 2026-06-24T05:15:00Z  
**Plane:** `brain-os/ssot/` (authored law)  
**Pair:** `brain-os/ssot/SOURCEA_PLANNING_AUTHORITY_CARD_LOCKED_v1.md`  
**Registry:** `data/sourcea-governance-ssot-registry-v1.json` (active/superseded — this file does not add rows)

---

## One sentence to anchor planning

> **Plan from SSOT v3 + Master Blueprint + reconciliation map; execute from blueprints + FBE + Cloud Forge Run; use v1.1/v1.3 PDFs as target shape, not as a rebuild spec.**

---

## ROI verdict (do not re-litigate v1.1)

Your current SSOT and blueprints are **well aligned** with ROI strategy — **more** than blindly finishing v1.1’s Cloudflare / Neon / SQL checklist would be.

The gap is not “wrong direction.” The gap is:

1. **Productize the proof** — Proof Pack + T1 paying client  
2. **Harden the motor** — executor + router on existing Railway FBE  

**Not:** replace what you already built.

**Execution order (founder LOCKED 2026-06-24):** Proof Pack seal **first** → T1 client → executor/router hardening.

---

## Mental model (law vs proof vs public)

| Plane | Role |
|-------|------|
| `brain-os/` | Truth **authored** — law, SSOT, planning cards (Git versioned) |
| `~/.sina/` | Truth **proven** — receipts, gates, machine output |
| `docs/` · `sites/` | **Public** half exposed |
| `apps/` · `scripts/` | Code lives with the app — never in the law folder |

Same separation as kernel: **contracts** (brain-os) ≠ **execution receipts** (`~/.sina`).

---

## PDF roles (registry-backed)

| PDF | Registry | Use |
|-----|----------|-----|
| v1.1 | `llm_agent_operating_law_v1` · **superseded** | Lineage · shape reference only |
| v1.2 | `llm_agent_operating_law_v2` · **superseded** | Lineage · shape reference only |
| v1.3 | `cloud_kernel_v1_3` · **active** | L1–L8 **target** — not a rip-and-replace order |

**Dispatch law (active):** `brain-os/ssot/SOURCEA_LLM_AGENT_OPERATING_LAW_SSOT_v3.md` — not v1.1 PDF.

---

*Planning ROI Anchor v1 · open with Planning Authority Card before any planning decision.*
