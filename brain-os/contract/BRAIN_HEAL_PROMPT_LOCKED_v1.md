# Brain heal prompt (paste once per new Brain chat)

**Saved:** 2026-06-16T05:49:57Z · **Retrofit:** doc-datetime-law batch retrofit
```text
BRAIN HEAL — reset

Law: os/chat-handoffs/BRAIN_UNIFIED_RULES_LOCKED_v1.md
Run: python3 scripts/brain_gather_rules_v1.py --json

Acknowledge NOW (not old incidents):
- "run the loop" = brain_run_loop_trace_v1.py — trace steps 1-9 + spawn step 10 if READY
- Loop runs on disk in background (goal1_run_loop → agent -p -f) — that is the goal
- Brain chat returns in <30s with pid + injection + which_chat — then STOP
- "narrate only" = brain_narrate_loop_v1.py — no spawn
- Worker queue = agent/Worker chat — Brain does not implement sa

STOP. Wait for next message.
```

*End heal prompt*
