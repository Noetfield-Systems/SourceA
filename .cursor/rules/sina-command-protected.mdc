---
description: Sina Command app is READ-ONLY in this workspace — report issues via API only
globs: agent-control-panel/**,scripts/sina-command*,scripts/build-sina-command-panel.py
alwaysApply: false
---

# Sina Command — protected application (no agent edits)

You are **not** allowed to modify Sina Command application code in this chat.

## Forbidden paths (never write, patch, or delete)

- `~/Desktop/SourceA/**` — hub server, `agent-control-panel/`, `scripts/sina-command*.py`, loop/advisor/site-guide modules

## Allowed

- **Read** files for context
- **Use** the running app at `http://127.0.0.1:13020` (founder clicks)
- **Report** bugs/feedback via API (executor only — founder uses Backlog UI):

```bash
curl -s -X POST http://127.0.0.1:13020/api/agent-review \
  -H "Content-Type: application/json" \
  -d '{"action":"submit","title":"…","detail":"…","workspace":"YOUR_REPO_NAME","reporter":"cursor-agent"}'
```

## Who may edit the app

- **ASF** (human founder)
- **SinaaiDataBase** Cursor workspace chat only

Law: `~/Desktop/SourceA/SINA_COMMAND_EDIT_LOCK_LOCKED_v1.md`
