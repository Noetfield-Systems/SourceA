---
description: Live wire sync at session start — not a mandatory reply dump
alwaysApply: true
version: 3
supersedes: 001-founder-zero-sina-command-name.mdc prohibition tables
---

# Live wire (session sync only)

**Law:** `AGENT_DISK_LIVE_WIRE_FIRST_LOCKED_v1.md`

## Session start

```bash
python3 scripts/agent_session_gate_run_v1.py --role <role> --json
```

Syncs truth bundle in background. **Do not paste it into chat.**

## Reply to founder

Answer their question. **Do not** quote `factory_now_line` or dump `agent-live-surfaces-v1.json` unless they ask for factory status.

## Do not

- Treat chat history or `command-data.json` as truth
- Hand-edit stale brain markdown without sync
- Reply like a database export
