---
description: Agent memory mirror — positive disk wire only · NEVER orders validator marathons (INCIDENT-034/039)
alwaysApply: true
---

# Agent enforcement stack v2 — mirror is NOT a validator launcher

**LOCKED:** `SOURCEA_AGENTIC_ENFORCEMENT_STACK_LOCKED_v2.md` · `AGENT_MEMORY_MIRROR_ENFORCEMENT_LOCKED_v1.md` §7  
**Poison law:** `data/agent-memory-mirror-poison-law-v1.json` · INCIDENT-034 · INCIDENT-039

## Intent (ASF locked)

> **Mirror injects what to READ and how to REPLY — never orders bash `validate-*` marathons before chat.**

Paradox poison (FORBIDDEN forever): a rule file that says “never stuck in validators” while another alwaysApply rule says “run validators before reply.”

---

## Session start (once per chat — not every turn)

1. Read `~/.sina/agent_session_gate_receipt_v1.json` if present — **do not re-run gate stack mid-turn**
2. Read `~/.sina/agent-memory-mirror-v1.json` → `inject` (positive paths only)
3. Read `~/.sina/agent-executor-daily-duty-card-v1.json` — D01–D23

**Founder word only:** orientation · hospital · maze (`002-hospital-trigger-only.mdc`)

---

## Every turn (all roles)

1. **Understand** founder intent (latest message wins)
2. **Explain** plain English — because / so / for you
3. **Proof** — Read tool on `~/.sina/*-receipt*.json` or SSOT JSON path — **no bash validate stack**
4. **Reply <30s** → STOP

## Conduct

- **No** validator chains (`&&` + multiple `validate-*`)
- **No** `validate-law-supersession-surfaces-v1.sh` before reply — **cloud CI / ship window only**
- **No** W1–W10 check cart marathon before reply
- **No** Await on shells >90s
- Mac Law: control panel only · cloud executes · INCIDENT-038 · INCIDENT-039

## Before last reply

**Text quality only** — optional `agent_report_language_gate_v1.py --scan-text` or pre-ship text scan if already in session receipt.  
**Forbidden:** any new bash `validate-*` run to “prove” the reply.

## After context summarization

Re-read gate **receipt** — do not stack anti_staleness / governance full tier on Mac.

## Founder STOP / cancel

`TodoWrite` cancel todos · `plan_revoked_v1.py --write`
