# Secrets — never commit
.env
.env.*
!.env.example
**/.env.local
**/.env.*.local
infra/secrets/*.env
infra/secrets/*.local
*.pem
*.key
id_rsa*

# Local secrets dir (outside repo — block if ever copied in)
.sourcea-secrets/
**/.sourcea-secrets/
.sina/secrets/
**/.sina/secrets/

# Supabase local CLI artifacts
.supabase/

# Python / test caches (safe to delete)
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db

# Build / deps / logs (keep TS + Git fast)
node_modules/
**/.next/
build/
*.log
.cursor/*
!/.cursor/rules/
!/.cursor/skills/
.cursor/debug-*.log
.cursor/_hub_probe.html
.cursor/handoff_state.md
apps/**/node_modules/

# dist is COMMITTED for Vercel static deploy (regenerate via build_sourcea_vercel_output_v1.py before push)

# Cleanup scan output may list paths — ok to commit report without keys
# infra/cleanup/secret-scan-report.txt

# Runtime receipts — cloud/Mac session churn (canonical truth in ~/.sina + cloud volume)
receipts/bays/
receipts/cloud-dispatch/
receipts/cloud-forge-run/
receipts/e2e-reports/
receipts/outreach/
receipts/valid-yes-progress-verdict-lock-*.json
receipts/**/*.jsonl
receipts/*-receipt-v1.json
!receipts/cloud/autorun-pending/
!receipts/cloud/autorun-pending/**
!receipts/cloud/external-verify/
!receipts/cloud/external-verify/**
receipts/*.png
reports/sourcea-dirty-tree-lane-report-v1.json

# Hub runtime projections — rebuilt by disk_live_wire_sync (do not commit drift)
agent-control-panel/command-data.json
agent-control-panel/command-data-runtime.json
agent-control-panel/command-data-shell.json
agent-control-panel/command-data-canonical.json
brain-os/plan-registry/SOURCEA-PRIORITY.md

!receipts/proof/
!receipts/proof/**
