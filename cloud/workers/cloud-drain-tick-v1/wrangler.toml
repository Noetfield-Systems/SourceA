name = "sourcea-cloud-drain-tick-v1"
main = "src/index.js"
compatibility_date = "2024-06-01"
account_id = "0d0b967b77e2e5535455d39ff3dae72c"

[triggers]
crons = ["*/10 * * * *"]

# Secrets (run once): bash scripts/cloud_drain_cf_secrets_v1.sh
# Or Cloudflare dashboard → Workers → sourcea-cloud-drain-tick-v1 → Settings → Variables
#   FBE_CLOUD_WORKER_URL = https://sourcea-fbe-runner-production.up.railway.app
#   FBE_INTERNAL_SECRET = (from ~/.sina/secrets.env)
#   CLOUD_DRAIN_AUTO_PROCEED = true
#   SUPABASE_URL = (portfolio-spine)
#   SUPABASE_SERVICE_ROLE_KEY = (portfolio-spine service role — CRON_FIRED writes only)

[observability]
enabled = true
