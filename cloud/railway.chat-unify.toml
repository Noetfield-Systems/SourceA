[build]
builder = "DOCKERFILE"
dockerfilePath = "cloud/Dockerfile.chat-unify"

[deploy]
startCommand = "/entrypoint.sh"
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
