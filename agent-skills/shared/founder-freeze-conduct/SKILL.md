---
name: founder-freeze-conduct
description: >-
  ASF STOP, FREEZE, bounded resume — factory spawn gate law. Use on stop, halt,
  freeze, interrupt, or Cloud Forge Run requests.
disable-model-invocation: true
---

# Founder freeze & conduct

## STOP (ASF_STOP class)

Run **first**:

```bash
python3 scripts/stop_goal1_auto_run_v1.py --json
```

Then quote `factory-now` from `~/.sina/factory-now-v1.json`.

**Do not** spawn autodrain, pack_loop, batch, or unified autorun.

## Resume (bounded only)

Founder: `ASF: Cloud Forge Run — max N — receipt required`

Maintainer:

```bash
python3 scripts/factory_control_v1.py resume --max-turns N
```

Never `rm ~/.sina/auto-run-disabled-v1.flag`.

## Factory motion

- **RUN INBOX** — one sa when Brain routes
- **Hub Actions** — validators/sync
- **Cursor AUTO-RUN** — does not exist
