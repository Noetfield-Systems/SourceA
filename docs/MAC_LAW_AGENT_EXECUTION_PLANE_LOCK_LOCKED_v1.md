# Mac Law Agent Execution Plane Lock — LOCKED FOREVER v1

**Version:** 1.1.0 · **Saved:** 2026-06-20T18:05:00Z · **Authority:** ASF lock order (INCIDENT-038 v1.1)
**Path:** `~/Desktop/SourceA/docs/MAC_LAW_AGENT_EXECUTION_PLANE_LOCK_LOCKED_v1.md`
**Machine SSOT:** `data/mac-law-agent-execution-plane-lock-v1.json`
**Lock flag:** `~/.sina/mac-law-agent-no-factory-on-mac-locked-v1.flag`

---

## 0. One law (AGENTS — locked forever)

> **Mac = control panel only. Cloud/API/FORGE/Railway executes ALL factory work. No factory building on Mac.**

**This is NOT** “founder never Terminal.” That is a separate UX rule.

**This is NOT** “Worker on Mac runs every plan.” **FORBIDDEN** (INCIDENT-038 v1.1). Worker does **not** factory-build on Mac. Secondary 5000  plans **never touch Mac.**

See `data/mac-worker-vs-factory-vocabulary-v1.json`.

---

## 1b. Mac control · secondary cloud · factory body (INCIDENT-038 v1.1)

| Lane | On Mac? | Role |
|------|---------|------|
| **Mac control** | YES | Hub · form · glance · optional ONE cloud dispatch POST · read receipts |
| **Secondary -5000** | **NO** | Cloud/API batch only — free tier · OpenRouter affordable |
| **Factory body** (any plan) | **NO on Mac** | Cloud/API only |

```text
Mac control (optional POST) → cloud tick → factory body → receipt → Mac reads ONLY
```

**Forbidden phrases (instant fail):**

- “Worker on Mac runs every plan”
- “RUN INBOX per sa-mkt” for secondary packs
- “cloud/FORGE only” without “Mac control only · Mac does not build”

---

## 1. Boss order

```text
ASF → MAC LAW → agent execution plane lock → universal wire → per-repo SSOT → cloud execution
```

---

## 2. What agents must never do on Mac body

| Forbidden | Route instead |
|-----------|---------------|
| `fbe_motor_delegate` | Cloud FBE / Railway |
| Local autorun worker | `loop_specialist_tick` → cloud dispatch |
| Playwright film render | Frozen · cloud CI |
| Local OpenRouter/fal factory spend | Cloud factory bay |
| Stacked E2E validators | Cloud CI / ship window |
| FORGE build when `mac_build_forbidden` | `cloud_forge` dispatch |

---

## 3. Triple sync (locked with Mac Law)

| Sync | Law |
|------|-----|
| Anti-staleness + vocabulary | Paired — never separate |
| Zero drift | session on Mac · full on cloud CI |
| Anti-fragmentation | one `queue_sa` everywhere |

---

## 4. Nerves + nodes (machine wired)

| Layer | Id / path |
|-------|-----------|
| Nerve orchestrator | `scripts/agent_nerve_system_v1.py` |
| Nerve nodes | `mac_law_universal_wire` · `mac_law_machine_enforce` · `mac_law_agent_lock` |
| Ship gate | `mac_law_agent_no_factory_on_mac` |
| Pipeline graph T0 | `mac_law_universal_wire` node |
| Session gate | `mac_law_universal_wire` step |
| Conduct gate | `mac_law_universal` + `mac_law_machine` scan |
| Pre-write | Mac Law universal check |
| Mirror inject | `mac_law_universal_detail` + `mac_law_agent_lock` |

---

## 5. Verify (forever)

```bash
python3 scripts/mac_law_agent_execution_plane_lock_v1.py --sync-receipt --enforce --json
bash scripts/validate-mac-law-agent-execution-plane-lock-v1.sh
python3 scripts/agent_nerve_system_v1.py --json
```

Quote on surfaces: `mac_law_agent_lock_line` · `mac_law_universal_line`

---

**End — LOCKED FOREVER v1**
