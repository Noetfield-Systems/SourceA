# SourceA Dirty Tree Lane Ownership (LOCKED v1)

**Saved:** 2026-06-27T09:53:00Z  
**SSOT:** `data/sourcea-dirty-tree-lane-map-v1.json`  
**Script:** `scripts/sourcea_dirty_tree_lane_map_v1.py`  
**Validator:** `scripts/validate-sourcea-dirty-tree-lanes-v1.sh`  
**Report:** `~/.sina/sourcea-dirty-tree-lane-report-v1.json`  
**Receipt:** `~/.sina/sourcea-dirty-tree-lane-map-v1.json`

---

## One Law

A dirty tree is not a blocker when an agent stays inside its assigned lane, checks path-scoped diffs, and never reverts unrelated work.

The problem is not that SourceA has many changed files. The problem is when runtime code, generated corpus, docs, incidents, model policy, and site assets are mixed without ownership.

---

## Required Agent Flow

1. Run the pre-agent lane report before plain `git status`.
2. Identify the lane before editing.
3. Run a path-scoped status/diff for the target files.
4. Touch only files in the assigned lane.
5. Do not revert unrelated dirty files.
6. If another lane is needed, stop and report the handoff.
7. Run the lane validator, not a full Mac validator marathon.

---

## Lane Map

| Lane | Owner | Scope |
|---|---|---|
| `brain_chat_runtime` | Brain runtime agent | Worker, retrieval, guardrails, Brain scripts, chatbot widget |
| `generated_brain_corpus` | Brain knowledge refresh agent | Crawled/distilled corpus, SQLite, knowledge bundles |
| `chat_unify` | Chat Unify agent | Chat Unify engine, router, catalog, `/unify` assets |
| `model_roi` | Model ROI agent | Model matrix, evals, router policy, model test runner |
| `intent_approval_machine` | Intent approval agent | Founder intent approval machine and receipts |
| `incidents_rules` | Incident/rule agent | Incident docs, registries, Cursor rules |
| `docs_plan_registry` | Docs agent | Locked docs, runbooks, plan registries |
| `site_assets` | Landing/site agent | Landing source/dist assets outside narrower lanes |
| `other_control` | Coordinator | Manual triage and cross-lane support files |

---

## Generated Corpus Freeze

`generated_brain_corpus` is frozen by default.

Agents must not touch these files unless the task explicitly says Brain knowledge refresh, corpus rebuild, crawl, distill, sync, or bundle update.

This lane includes:

- `data/chatbot-knowledge/**`
- `data/brain-chat-knowledge-bundle-v1.json`
- `cloud/workers/sourcea-brain-chat-v1/src/knowledge-bundle.json`
- Brain public source/rule/index JSON generated by distillation

---

## Commands

Pre-agent status:

```bash
python3 scripts/sourcea_dirty_tree_lane_map_v1.py --write-report --json
```

This writes to `~/.sina` so the status check does not dirty the repo.

Classify current dirty tree:

```bash
python3 scripts/sourcea_dirty_tree_lane_map_v1.py --json
```

Write report and receipt:

```bash
python3 scripts/sourcea_dirty_tree_lane_map_v1.py --write-report --json
```

Classify paths:

```bash
python3 scripts/sourcea_dirty_tree_lane_map_v1.py \
  --path scripts/model_dispatch.py \
  --path data/chatbot-knowledge/brain_knowledge_v1.sqlite \
  --json
```

Check proposed paths before editing:

```bash
python3 scripts/sourcea_dirty_tree_lane_map_v1.py \
  --lane model_roi \
  --check-paths data/sourcea-ai-model-roi-test-matrix-v1.json scripts/model_dispatch.py
```

Validate lane system:

```bash
bash scripts/validate-sourcea-dirty-tree-lanes-v1.sh
```

---

## What Agents Should Say

Good:

```text
I am working in lane `model_roi`; path check is clean; I will ignore unrelated Brain corpus changes.
```

Bad:

```text
The tree is dirty, so I cannot proceed.
```

Bad:

```text
I will clean the tree first.
```

Cleaning means reverting or reshaping someone else's work. That is forbidden unless explicitly requested.

---

## Handoff Rule

If the assigned task crosses lanes, report it plainly:

```text
This needs `brain_chat_runtime` plus `generated_brain_corpus`. I can edit runtime now, but corpus refresh needs a knowledge-refresh lane handoff.
```

This keeps every other agent satisfied: each agent gets bounded ownership, proof, and no surprise edits.

---

## Known External Blocker

The git dirty-tree problem is solved by lane commits and lane reports. The remaining large-system blocker is Supabase access for the SourceA cloud data chain.

See: `docs/SOURCEA_SUPABASE_CONNECTION_BLOCKER_LOCKED_v1.md`

Short version: founder must recover Supabase dashboard access and fill `~/.sourcea-secrets/portfolio-spine.env` before agents can connect the 7000+ plan registry to Supabase, Cloudflare Workers, Railway cloud workers, Brain, Hub Pro, and Chat Unify.

