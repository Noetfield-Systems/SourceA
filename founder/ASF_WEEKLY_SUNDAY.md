# Sunday planning only — ASF

**Human planning day** in Prompt OS settings. Weekdays = execute, not re-architect.

---

## Sunday (~30–60 min)

1. Read `SourceA/ARCHITECT_REPORT.yaml` (after `./scripts/run-architect.sh` if needed)
2. Skim `SourceA/GLOBAL_BLOCKERS.json` + `GLOBAL_PRIORITY.json`
3. Adjust repo `os/plan.json` **next_tasks** if needed (ASF decision)
4. Optional: `cd ~/Desktop/SinaPromptOS && ./scripts/run-full-cycle.sh`

---

## Not on Sunday only

- Daily `dispatch-day.sh` + paste + ingest — **every work day** (`ASF_DAILY_CARD.md`)
- Wire proof — any day when ready
- Repo IMPLEMENT sessions — any day

---

## Phase / goal map (one line each)

| Code | Meaning |
|------|---------|
| P2 | Wire RUN SYSTEM — close with full_m8 + G3 |
| P-R / G4 | App Store (Cursor OS Pro) |
| M8 | Internal automation (UI locked safe mode) |
| phase_0_exit | Mono governance — ASF picks 1A/1B/1C |

Full map: fast-track plan in Cursor or `SINAAI_ECOSYSTEM_FINAL_STATE_AND_NEXT_PLAN_LOCKED_v1.md`
