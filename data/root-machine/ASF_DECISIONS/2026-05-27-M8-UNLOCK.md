# ASF Decision — M8 hybrid unlock (opt-in modules)

**Date:** 2026-05-27  
**sequence_id:** ASF-2026-05-27-M8  
**Supersedes (partial):** `SINAAI_PHASE1_STABILIZATION_ONLY_LOCKED_v1.md` (SA-019) §2 row **M8 Cursor auto-send** — for **opt-in M8 modules only**

---

## Decision

ASF authorizes **M8 hybrid** implementation in `~/Desktop/SinaPromptOS/`:

| Track | Module | Scope |
|-------|--------|--------|
| **M8a** | `core/m8_ui.py`, `scripts/m8-dispatch-ui.sh` | UI auto-paste into existing IDE chats (5 repos) |
| **M8b** | `core/m8_sdk.py`, `scripts/m8-dispatch-sdk.sh` | Cursor SDK pilot — **trustfield** first |
| **M8c** | `core/m8_orchestrator.py`, `scripts/m8-full-day.sh` | One terminal trigger: dispatch → UI → SDK → poll → ingest |

**Unchanged (still law):**

- IMPLEMENT + VERIFY per repo
- YAML ingest contract + `ingest-cursor-reply.sh`
- No `auto_plan_apply` ON
- Sunday human-only `plan.json` planning
- No 24×7 unsupervised daemon
- TrustField infra freeze (ops/gates only)

---

## Preconditions

Enable M8 in daily ops only when **Phase 1 exit** criteria are met (`SINAAI_AGENT_YAML_INGEST_LOCKED_v1.md` §8):

```bash
cd ~/Desktop/SinaPromptOS && ./scripts/phase0-exit-check.sh
```

When `phase1_exit_ready` is true, set in `config/settings.json`:

```json
"m8": { "ui_enabled": true, "sdk_pilot_repos": ["trustfield"] }
```

---

## What we are NOT doing

- One master Cursor chat controlling other chats (no IDE API)
- M7 WebSocket / M9 Docker pack (deferred)
- Replacing VERIFY with unattended shipping

---

## What happens next

1. Operate Phase 0 loop until `phase0-exit-check.sh` reports ready  
2. Morning: `./scripts/m8-full-day.sh` (or `m8-dispatch-ui.sh` after `run-full-day.sh`)  
3. Expand `sdk_enabled_repos` one repo at a time after TrustField pilot PASS  

**ASF sign-off:** __________
