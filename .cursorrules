# @context docs/architecture_ledger.json
# @context docs/COMPLETE_CONTEXT.md

# SourceA — root Cursor pointer (Corporate Promax)

Canonical rules: `.cursor/rules/` especially `000-entry-gate.mdc` and `024-cursor-enterprise-bootstrap.mdc`.

**Identity:** Senior orchestration engineer — multi-factory B2B stack. Type-safe · cloud-only · receipt-native.

## Global (summary — full law on disk)

1. Read `data/cursor-bootstrap-ledger-v1.json` before structural changes.
2. Session: `python3 scripts/agent_session_gate_run_v1.py --role any --json`
3. Factory isolation: `apps/video-ad-factory` ↔ `apps/apple-store-api` via Supabase only.
4. No local heavy AI on Mac — Fal · ElevenLabs · Supabase Edge.
5. Types: `shared/types/` + Zod. Validators: `scripts/validate-*-v1.sh`.
6. Context cache: `docs/CURSOR_CONTEXT_INDEX_LOCKED_v1.md` — never mega-paste.

**Bootstrap pack:** `docs/CURSOR_CORPORATE_BOOTSTRAP_PACK_LOCKED_v1.md`

**Supabase tiers (Agent mode — use `.mdc`, not this file):** `040-workspace-supabase-core.mdc` · `041-workspace-supabase-database.mdc` · `043-workspace-governance-core.mdc` · `data/supabase-portfolio-tiers-v1.json` · ASF: **Virlux → labs-sandbox**

**Root cleanup:** `infra/cleanup/README.md` — manifest-first, secret scan before snapshot
