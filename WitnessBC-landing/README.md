# Moved → `../witnessbc-site/`

Witness BC commercial landings now live in **`witnessbc-site/`** — fully self-contained, E2E build.

```bash
bash ../witnessbc-site/open.sh
```

This folder is deprecated. Do not add new files here.
