# Graph Studio Web (slice-1)

React Flow Studio UI for NF-GRAPH-STUDIO-V1. Served as Workers Assets from `sourcea-graph-studio-v1`.

Layout positions never enter `plan_hash`. Publish strips layout before compile.
