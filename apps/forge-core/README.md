# Forge Core — shared runtime contract

Shared TypeScript package for Forge Production MVP: Zod schemas, file-backed state store, BullMQ queue helpers.

See `infra/forge-mvp/README.md` for full loop wiring.
