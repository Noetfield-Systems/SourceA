name: validate-sourcea-boot-v1
on:
  push:
    branches: [main]
  pull_request:
concurrency:
  group: validate-sourcea-boot
  cancel-in-progress: false
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "sourcea-boot package validation"
