name: external-verify
on:
  push:
    branches: [main]
  workflow_run:
    workflows: [deploy-chain-v1]
    types: [completed]
  workflow_dispatch:
concurrency:
  group: l4-external-verify
  cancel-in-progress: false
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "L4 external verify lane — 15-recipe matrix inside this workflow"
